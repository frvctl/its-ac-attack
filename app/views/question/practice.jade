script
  $(document).ready(function(){
    $("#buzzer").click(function(event){
      $("#answerDiv").removeClass("hide");
      $("#buzzer").addClass("hide");
      });
    });


div
  div.row-fluid
    each item, i in question
      ul
        form.well
          li(type="none")
            != 'Difficulty: '
            != item.difficulty
            != ' | '
            != 'Year: '
            != item.year
            != ' | '
            != 'Category: '
            != item.category
            != ' | '
            != 'Tournament: '
            != item.tournament
          hr
          li(type="none")
            != 'Question: '
            != item.question
      
      p= item.answer

      p ANSWER CHECKING OUT OF COMMISION TEMPORARILY

  form(name="answerForm", action="", method="get").center.well.search-form
    div
      input(type='text', name='answerInput', placeholder='Answer').input-xlarge.search-query
    button(type='submit').btn.btn-primary Answer
